# CHANGELOG — PaperClip2

> Une ligne par mission terminée. Format : `[date] | [type] | [description]`

---

| Date | Type | Description |
| --- | --- | --- |
[2025-01-17] | Film Tracker - Page statique | Création de page web statique avec formulaire (titre, genre, statut), liste vide par défaut, design sombre avec cartes blanches et bordures colorées, typographie Inter, aucun JavaScript. | index.html, style.css
- 2026-05-16 : CADRAGE — J'ai un projet web "Film Tracker" pour suivre les ... (réflexion #12, livrable mission_code) — en attente d'exécution
- 2026-05-16 : CADRAGE — J'ai un projet web "Film Tracker" pour suivre les ... (réflexion #11, livrable mission_code) — en attente d'exécution
- 2026-05-16 : CADRAGE — j'aimerais réfléchir à comment ajouter une nouvell... (réflexion #2, livrable mission_code) — en attente d'exécution
| 2026-04-14 | Outillage | Initialisation Graphify — création `.graphifyignore`, activation hook always-on via `graphify claude install` (fichiers `.claude/settings.json` et `CLAUDE.md` vérifiés), génération `graphify-out/GRAPH_REPORT.md` + `graphify-out/graph.json` + `graphify-out/graph.html`, ajout `graphify-out/` dans `.gitignore`, mise à jour `PROJET_CONTEXTE.md` (règles, décision figée, statut session) |
| 2026-04-13 | Gameplay Marché | CONNECT-MARKET — branchement des upgrades `reputation` (bonus demande ligne 312-314), `marketResearch` (réduction volatilité lignes 264-267), `quality` (bonus prix effectif lignes 369-372) au runtime de `MarketManager`, application des bonus niveau joueur `productionMultiplier` (+5%/niveau lignes 62-63, 179-181) et `salesMultiplier` (+3%/niveau lignes 325-326) depuis `LevelSystem.productionMultiplier/salesMultiplier`; build debug APK validé |
| 2026-04-13 | Gameplay Production | CONNECT-PRODUCTION — branchement des upgrades `speed`/`bulk`/`efficiency` au runtime de production (`ProductionManager.processProduction()` et `currentProductionRatePerSecond`), combinaison multiplicative des bonus recherche + upgrade pour éviter dépassement 100%, utilisation de `GameConstants.AUTOCLIPPER_COST_MULTIPLIER` au lieu du `1.15` hardcodé (ligne 300), passage de `_autoSellEnabled` à `false` par défaut avec sérialisation JSON ajoutée; build debug APK validé |
| 2026-04-13 | Gameplay Auto Metal | AUTO-METAL-PURCHASE — câblage `UNLOCK_FEATURE` pour `auto_metal_purchase` vers `PlayerManager.enableAutoMetalBuyer()`, ajout de l'achat auto passif dans `GameState.tick()` (stock < 30%, argent > 100€, prix effectif < `METAL_AUTO_BUY_PRICE_MAX`), ajout constante `GameConstants.METAL_AUTO_BUY_PRICE_MAX = 0.40`; build debug APK validé |
| 2026-04-13 | Corrections techniques | CORRECTIONS-TECHNIQUES-V1 — migration des usages combo XP vers `XPConfig` (`COMBO_MAX`, `COMBO_INCREMENT`) et suppression des constantes XP redondantes de `GameConstants`; alignement de `ResourceManager.canPurchaseMetal()` sur la formule de prix effective (discount upgrade procurement + research) identique à `GameState.effectiveMetalUnitPrice`; build debug APK validé |
| 2026-04-13 | Gameplay Agents | CHANTIER-04-AGENT-EFFICIENCY — synchronisation immédiate des déblocages agents/slots après achat de recherche via écoute des changements `ResearchManager` dans `AgentManager`, et application du bonus `agentEfficiency` sur l'intervalle effectif d'exécution des agents (`actionIntervalMinutes * (1.0 - efficiency)`); build debug APK validé |
| 2026-04-13 | Gameplay Reset | CHANTIER-05-RESET-COMPLETION — flux prestige complété : reset arbre recherche via `researchManager.resetForProgression()` (conservation META), enregistrement `recordReset(...)` pour incrément `totalResets` + historique, application bonus META Quantum/Innovation via `getResetBonus('quantum'/'innovation')`; build debug APK validé |
| 2026-04-13 | Refactor Architecture | CLEANUP-ARCHITECTURE-V1 — suppression code legacy (14 fichiers supprimés), retrait références StartScreen/MissionSystem/compétitif obsolète, nettoyage imports et constantes mortes, build debug APK validé |
| 2026-04-12 | Correction bug Données | Suppression entreprise cloud fiabilisée pour IDs legacy (suppression validation UUID au delete), purge Firestore `enterprises` exécutée pour repartir à zéro, fichier modifié : `lib/services/cloud/cloud_persistence_adapter.dart` |
| 2026-04-12 | UI Navigation | Navigation mobile simplifiée — barre du bas en icônes seules (`NavigationDestinationLabelBehavior.alwaysHide`) en mode normal et crise avec hauteur 52, ajout d'une 3e ligne subtile dans l'AppBar mobile affichant le nom du panel actif, hauteur AppBar mobile alignée à 124 (`GameAppBar` + `AppScaffold`) |
| 2026-04-12 | UI/UX Texte | Nettoyage StartScreen & traces mondes — bouton `Créer une entreprise` masqué si sauvegarde existante, overflow du libellé `Continuer` corrigé avec ellipsis, textes UI `Nouveau monde/mondes` remplacés par `Nouvelle partie/entreprise`, message orchestrator aligné sur entreprise |
| 2026-04-12 | Gameplay UI/Core | Finition cohérence prix — MetalBuyer passe par `GameState.purchaseMetal()` avec comparaison du prix effectif, ajout de `productionBulk` dans `activeProductionBonuses`, masquage des lignes bonus à `0%` dans les panels Production/Marché avec message neutre en absence de bonus |
| 2026-04-12 | Gameplay UI/Core | Cohérence prix & effets recherche — centralisation des prix effectifs dans `GameState` (`effectiveMetalUnitPrice`, `effectiveAutoclipperCost`), unification coût autoclipper dans `ProductionManager` (upgrade automation + recherche), affichage des bonus actifs dans panels Production/Marché/Recherche, suppression écrans morts (`production_screen.dart`, `market_screen.dart`, `upgrades_screen.dart`) |
| 2026-04-10 | Reprise projet | Nettoyage et alignement sur METHODO — création PROJET_CONTEXTE.md, archivage documentation |
| 2026-04-10 | Nettoyage worldId | 0 occurrence `worldId` dans lib/ — 30 occurrences remplacées par `enterpriseId` dans `game_persistence_orchestrator.dart` |
| 2026-04-10 | Auth & Cloud | Fiabilisation flux auth/cloud — suppression `cloud_enabled` (cloud automatique si Firebase connecté), centralisation appels auth via `AppBootstrapController.requestGoogleSignIn()`, déclencheur sync unique (listener Firebase Auth), 9 fichiers modifiés |
| 2026-04-10 | Bug fix Android | AUTH-ANDROID-FIX — Google Sign-In `clientConfigurationError` corrigé : `GoogleSignIn.instance` → instance statique avec `serverClientId` explicite (google_sign_in v7+ ne lit plus google-services.json automatiquement), 1 fichier modifié |
